services:

  mysql:
    container_name: mysql
    image: mysql
    restart: always
    environment:
      MYSQL_DATABASE: mpsp
      MYSQL_ROOT_PASSWORD: password
    ports:
     - "3306:3306"

  redis:
    container_name: redis
    image: redis
    restart: always
    ports:
     - "6379:6379"
